import { WalkGenderPipe } from './walk-gender.pipe';

describe('WalkGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new WalkGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
