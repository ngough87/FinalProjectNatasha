import { WalkCategoryPipe } from './walk-category.pipe';

describe('WalkCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new WalkCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
