import { WalkLocationPipe } from './walk-location.pipe';

describe('WalkLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new WalkLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
